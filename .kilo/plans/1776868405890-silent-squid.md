## Plan: Verify project name from package.json

**Goal**: Read `package.json` and report the project name, indicating success or errors.

### Steps

- [ ] Ensure `package.json` exists at `S:/self-organizing-library/package.json`.
- [ ] Read the file contents.
- [ ] Parse JSON and extract the `name` property.
- [ ] If reading or parsing fails, capture the error message.
- [ ] Return a structured response containing:
  - `read_success`: boolean
  - `project_name`: string (or `null` on error)
  - `error`: error message if any

### Verification
- Confirm that the file was read without I/O errors.
- Validate that the extracted `name` matches the value in the file.
- Ensure any error is clearly reported.

### Expected Output Example
```
Read Success: true
Project Name: nextjs-template
Errors: none
```