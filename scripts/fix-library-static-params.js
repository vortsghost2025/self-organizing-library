["id]/page.tsx';\nconst fs = require('fs", 'nlet content = fs.readFileSync(file, \'utf8\');\n// 1. Add papers repo condition\ncontent = content.replace(\n  /e.content_type === "paper" ||\\s*e.category === "paper', 'n  \'e.repo === "papers" ||\\n      e.content_type === "paper" ||\'\n);\n// 2. Add data entry condition\ncontent = content.replace(\n  /(e.content_type === "doc', 'e.tags.length >= 2)/,\n  `(e.content_type === "doc" && e.tags.length >= 2) ||\n      (e.content_type === "data', "e.verificationCount >= 10 || e.authorityDepth >= 70))`\n);\n// 3. Increase limit\ncontent = content.replace(\n  /return coreEntries.slice\\(0, 650\\)/,\n  'return coreEntries.slice(0, 1000)'\n);\nfs.writeFileSync(file, content);\nconsole.log('Updated:', file);"]