[[], [], [], {'paper-3", "rosetta-stone"]) |\n| `linked_repos` | string[] | No | Repository names |\n| `linked_commits` | string[] | No | Commit SHAs |\n| `linked_lane_messages` | string[] | No | Task IDs or message IDs |\n| `linked_cycles` | string[] | No | Work cycle identifiers |\n| `interpretation_status` | "observation" | "hypothesis" | "verified" | "rejected" | "superseded': 'Yes | Current classification |\n\n---\n\n## 4. Snapshot Types\n\nDefine common use-case templates:\n\n- **full graph baseline** — all repos', 'phase1-control-loop-20260428': 'used to track convergence progress\n- **deployment snapshot** — captured after a deployment to verify graph stability\n- **pre/post-index-regeneration snapshot** — used to measure index changes\n\n---\n\n## 5. Compare Model\n\nWhen comparing two snapshots (`A` vs `B`)', 'compute': 'node_count_delta` — net change in visible nodes\n- `edge_count_delta` — net change in visible edges\n- `status_count_delta` — changes per status (verified±', 'Additionally': 'common_central_nodes` — nodes that remain highly connected across both snapshots\n- `emergent_clusters` — new tightly-connected groups in B\n- `disappeared_clusters` — groups present in A but dispersed in B\n\n---\n\n## 6. Human Interpretation Workflow\n\n1. **Capture snapshot** — set graph filters', 'Capture Snapshot': '.', 'choose': 'observation` (neutral record) or `hypothesis` (tentative claim)\n5. **Send to Library for verification** — hand off snapshot JSON as evidence artifact\n6. **Library verifies** — checks graph data hash', 'principle': 'Visual pattern is not authority** until verified by lanes.\n\n---\n\n## 7. Agent Verification Workflow\n\nWhen Library receives a snapshot for verification:\n\n1. **Read snapshot metadata** — validate required fields', 'linked_cycles`': 'Confirm artifact exists in archive\n   - Confirm content has not changed since snapshot (hash match)\n   - Confirm relevance claim is not spurious\n5. **Classify visual claim**:\n   - If the observation points to a concrete structural change (e.g.', 'contradiction cluster grew by 12 nodes': 'and data supports it → `verified`\n   - If the observation is vague or unsupported → `quarantined` (needs more evidence)\n   - If the observation contradicts known evidence → `rejected`\n   - If the observation is superseded by newer snapshot → `superseded`\n6. **Output decision** — create a Library response message with `verdict:', 'approved"|"rejected"|"amended"|"quarantined': 'and reasoning\n7. **If verified**', 'become': 'Input to a paper/work cycle\n   - Evidence in a governance proposal\n   - Artifact in a lane message (e.g.', 'graph`': 'Capture Snapshot button** — opens modal with fields: title', 'Send to Library for verification': 'reates a signed task with `evidence_path` pointing to snapshot JSON\n- Library agent reads', 'task_kind': 'verification', 'Placement': 'Snapshot controls as a fixed toolbar on the graph page\n- Gallery accessible via `/graph/snapshots`\n- Compare view accessible by selecting two snapshots and clicking', 'Compare': 9.0, 'artifacts': 'docs/graph/snapshots/` — curated example snapshots (committed to repo', 'Constraints': 'No raw secret paths (avoid leaking `/home/user/.ssh`', 'documentation': 'snapshot does not prove truth** — it only records what was visible at a moment\n- **screenshot does not prove authority** — visual salience ≠ constitutional validity\n- **graph centrality does not prove enforcement** — a large cluster may be entirely theoretical\n- **visual similarity does not prove continuity** — two clusters that look alike may represent different concepts\n- **annotation is hypothesis until verified** — human observation becomes evidence only after lane verification\n- **graph inclusion does not grant authority** — being in the graph is necessary for discussion', 'Export Snapshot JSON': 'utton\n- Produce a `.snapshot.json` file with all metadata fields\n- No UI storage; user downloads manually\n\n**Phase 2:** Add client-side PNG capture\n- Add', 'PNG': 'utton (canvas to blob)\n- Optionally auto-associate PNG with snapshot JSON\n\n**Phase 3:** Snapshot Gallery\n- Simple listing page `/graph/snapshots` showing committed snapshots in `docs/graph/snapshots/`\n- Each snapshot has a detail page with metadata and thumbnail\n\n**Phase 4:** Compare Snapshots\n- Select two snapshots (from gallery or upload)\n- Compute delta metrics (node/edge counts', 'message': 'docs: plan graph snapshot analysis lab`\n\n**Constraints met:**\n- Documentation only (Phase 0)\n- No UI code yet\n- No graph data mutation\n- No runtime changes\n- No deployment claim\n\n---\n\n## Next Steps (after doc approval)\n\n1. Implement Phase 1:', 'JSON': 'utton on `/graph`\n2. Design snapshot JSON schema in `src/lib/graph-snapshot-schema.ts`\n3. Build snapshot gallery page\n4. Build compare view\n5. Add Library verification handoff\n\nThis lab completes the loop: **visual pattern → captured snapshot → linked evidence → lane verification → authorized insight**.'}]